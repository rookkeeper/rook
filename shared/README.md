# Shared contracts

Cross-package contracts and headless protocol/domain definitions shared during the client migration.

Current scope:
- ACP / JSON-RPC types
- environment DTOs
- agent/session DTOs

This directory is intentionally small for now. UI code stays in `client/`; server runtime code stays in `agent-server-client/`.
