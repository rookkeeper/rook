# clients-next/mac

Minimal next-generation macOS client bootstrap.

Right now this is just a tiny app shell that renders the shared `HelloWorldScreen` from `clients-next/RookKit/` so we have a clean place to start rebuilding the Mac client.
