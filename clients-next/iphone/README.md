# clients-next/iphone

Minimal next-generation iPhone client bootstrap.

Right now this is just a tiny app shell that renders the shared `HelloWorldScreen` from `clients-next/RookKit/` so we have a clean place to start rebuilding the iPhone client.
